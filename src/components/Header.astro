---
import { Image } from "astro:assets";

// 1. **IMPORTA** la imagen del logo directamente
import logo from '../assets/logo.png'; 


---

<header class="flex items-center justify-between px-6 py-4 z-50 glass-effect">
    
    <div class="flex items-center gap-2">
        <div class="h-28 w-28 bg-primary/20 rounded-lg flex items-center justify-center">
            <Image src={logo} alt="Misu Parfum Logo" width={400} height={400} /> 
        </div>
        <h1 class="text-lg font-display font-bold tracking-widest uppercase text-slate-800 hidden sm:block">Misu Parfum</h1>
    </div>
    <h1 class="text-xl font-display font-bold uppercase text-slate-800 sm:hidden">Misu parfum</h1>
    <div class="flex items-center">
       
    </div>

</header>